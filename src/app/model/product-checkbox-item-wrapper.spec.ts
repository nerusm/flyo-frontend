import { ProductCheckboxItemWrapper } from './product-checkbox-item-wrapper';

describe('ProductCheckboxItemWrapper', () => {
  it('should create an instance', () => {
    expect(new ProductCheckboxItemWrapper()).toBeTruthy();
  });
});
