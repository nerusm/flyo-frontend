import { ProductCheckboxItem } from './product-checkbox-item';

describe('ProductCheckboxItem', () => {
  it('should create an instance', () => {
    expect(new ProductCheckboxItem()).toBeTruthy();
  });
});
